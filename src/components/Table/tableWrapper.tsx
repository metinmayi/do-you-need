import styled from "styled-components";

const TableWrapper = styled.table`
  padding: 0.25rem;
  text-align: center;
  color: black;
  background-color: var(--borderColor);
`;

export { TableWrapper };
